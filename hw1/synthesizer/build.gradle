apply plugin: 'java'
apply plugin: 'application'

sourceSets {
    main {
        java {
            srcDirs 'synthesizer'
        }
    }
}

mainClassName = "TestArrayRingBuffer.class"

defaultTasks 'compileJava', 'run'
